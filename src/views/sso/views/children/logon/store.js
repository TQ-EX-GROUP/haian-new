export const SSO_LOGON  = 'SSO_LOGON'

export const api = {

}

const state = {}

const actions = (http) => ({ // eslint-disable-line
})

const mutations = {}

const getters = {}

export default (http) => ({
  namespaced: true,
  state,
  actions: actions(http),
  mutations,
  getters,
})
